<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fatimamostafa.animationsntransitions">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".animationtypes.ViewAnimationActivity" />
        <activity android:name=".animationtypes.DrawableAnimationActivity" />
        <activity android:name=".animationtypes.PropertyAnimationActivity" />
        <activity android:name=".animationtypes.ObjectAnimatorActivity" />
        <activity android:name=".animationtypes.InterPolatorActivity" />
        <activity android:name=".transitions.crossfade.CrossfadeTransitionActivity" />
        <activity android:name=".transitions.crossfade.DetailActivity" />
        <activity android:name=".transitions.viewpager.ViewpagerActivity"></activity>
    </application>

</manifest>